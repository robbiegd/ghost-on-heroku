<!DOCTYPE html>
<html lang="{{@site.lang}}">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<title>{{@site.title}} — {{meta_title}}</title>
		<meta name="HandheldFriendly" content="True">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		{{>services/google_fonts}}
		<link rel="stylesheet" type="text/css" href="{{asset "css/screen.css"}}">
		{{ghost_head}}
	</head>
	<body class="custom-subscribe{{#get "tags" limit="all" filter="visibility:internal" as |tags|}}{{#tags}} global-{{slug}}{{/tags}}{{/get}}">
		<div class="custom-wrap global-flex">
            {{#post}}
			{{#if feature_image}}
			<div class="custom-image global-bg-image" style="background-image: url({{feature_image}})"></div>
			<div class="global-cover global-bg-image{{#if feature_image}} custom-cover{{/if}}" style="background-image: url({{feature_image}})"></div>
			{{else}}
			{{#if @site.cover_image}}
			<div class="global-cover global-bg-image" style="background-image: url({{@site.cover_image}})"></div>
			{{/if}}
			{{/if}}
			<div class="custom-flex global-flex">
				<div class="custom-header">
					<a href="{{@site.url}}" class="custom-heading global-heading">{{>icons/site/arrow}}{{t "Back to Homepage"}}</a>
				</div>
				<div class="custom-content global-flex">
					<div class="subscribe-content">
						<form data-members-form="subscribe" class="subscribe-form">
							<h3 class="subscribe-title">{{title}}</h3>
							<div class="subscribe-ityped">
								<span id="ityped"></span>
							</div>
							<input data-members-email class="subscribe-input global-input" type="email" name="email" placeholder="{{t "Your email address"}}" aria-label="{{t "Your email address"}}" required>
							<button class="subscribe-button global-button" type="submit">{{t "Subscribe"}}</button>
							<script>
							const iTypedText = ['{{t "Start your day with {site_title}" site_title=@site.title}}'];
							</script>
						</form>
						{{>members_subscribe_alerts}}
						<div class="subscribe-success">
							<h3 class="subscribe-title">{{t "Great!"}}</h3>
							<p>{{t "Check your inbox and click the link to confirm your subscription"}}</p>
							<a href="{{@site.url}}" class="global-button">{{t "Back to Homepage"}}</a>
						</div>
					</div>
				</div>
				{{/post}}
				{{#if @site.description}}
				<div class="custom-footer">
					<a href="{{@site.url}}">{{@site.title}}.</a>
					<span>{{@site.description}}.</span>
				</div>
				{{/if}}
			</div>
		</div>
		<script src="{{asset "js/global.js"}}"></script>
		<script>
		document.addEventListener("DOMContentLoaded", function() {
			const body = document.body;
			body.classList.add("is-loaded")
		})
		</script>
		{{ghost_foot}}
	</body>
</html>